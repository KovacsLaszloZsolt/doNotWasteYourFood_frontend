import React, { ReactElement } from 'react';
import { Text } from 'react-native-paper';

const ManageCategories = (): ReactElement => {
  return <Text>Manage categories</Text>;
};

export default ManageCategories;
