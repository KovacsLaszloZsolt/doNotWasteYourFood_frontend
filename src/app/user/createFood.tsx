import { ReactElement } from 'react';
import { CreateFood } from '../../components/CreateFood/CreateFood';

const CreateFoodPage = (): ReactElement => {
  return <CreateFood />;
};

export default CreateFoodPage;
